# DDoS HTTP Flood
alert tcp any any -> $HOME_NET 80 (msg:"DDoS HTTP Flood Detected"; flags:S; content:"GET"; http_method; threshold:type both, track by_src, count 100, seconds 10; sid:1000012; rev:1;)  JOSS

alert tcp any any -> $HOME_NET 80 (msg:"DDoS HTTP Flood Detected"; flow:to_server,established; content:"GET"; http_method; threshold:type both, track by_src, count 100, seconds 10; sid:1000012; rev:2;)

alert tcp any any -> $HOME_NET 80 (msg:"HTTP attack detected"; content:"GET"; nocase; http_method; sid:1000003; rev:1;) JOSS




# DDoS SYN Flood
alert tcp any any -> $HOME_NET any (msg:"SYN Flood Attack Detected"; flags:S; threshold:type both, track by_dst, count 50, seconds 10; sid:1000011; rev:1;) JOSS

alert tcp any any -> $HOME_NET any (msg:"SYN Flood Attack Detected"; flags:S; threshold:type threshold, track by_src, count 50, seconds 10; sid:1000011; rev:1;)


alert tcp any any -> $HOME_NET any (flags:S; msg:"SYN flood detected"; detection_filter:track by_src, count 50, seconds 1; sid:1000002; rev:1;)




# DDoS ICMP Flood
alert icmp any any -> $HOME_NET any (msg:"DDoS ICMP Flood Detected"; threshold:type both, track by_dst, count 50, seconds 10; sid:1000013; rev:1;)

alert icmp any any -> $HOME_NET any (msg:"ICMP flood detected"; detection_filter:track by_src, count 50, seconds 10; sid:1000013; rev:1;) JOSS




# SSH Brute Force Attack
alert tcp any any -> $HOME_NET 22 (msg:"SSH Brute Force Attack Detected"; flags:S; threshold:type both, track by_src, count 5, seconds 10; sid:1000014; rev:1;)  JOSS

alert tcp any any -> $HOME_NET 22 (msg:"SSH Brute Force Attack Detected"; detection_filter:track by_src, count 5, seconds 60; sid:1000014; rev:1;)








